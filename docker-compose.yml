version: '3.8'

services:
  app:
    build: .
    container_name: company-app
    ports:
      - "8080:80"   
    volumes:
      - .:/var/www/html
    networks:
      - company-network

networks:
  company-network:
    driver: bridge
